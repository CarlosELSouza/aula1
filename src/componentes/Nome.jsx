import React from "react";

export default props => //já tá exportando já

    <div>
        {props.nome}   
        <strong>{props.sobrenome}</strong>  
        {props.endereco}
        {props.numero}
        {props.cidade}
        {props.estado}   
    </div>